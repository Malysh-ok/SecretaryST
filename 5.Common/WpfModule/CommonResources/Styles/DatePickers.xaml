<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/CommonResources;component/Styles/DefaultColorsAndBrushes.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!-- Стиль, используемый в DatePicker по умолчанию -->
    <Style TargetType="{x:Type DatePickerTextBox}">
        <!-- <Setter Property="Padding" Value="0 1 0 0"/> -->
        <Setter Property="Margin" Value="0 1 0 0"/>
    </Style>

    <!-- _______________ БАЗОВЫЙ DatePicker _______________ -->
    <!-- Стиль -->
    <Style x:Key="BaseDatePicker" 
           TargetType="{x:Type DatePicker}">
        <Setter Property="Height" Value="26"/>
        
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        
        <Setter Property="BorderThickness" Value="1" /> 
        <!-- <Setter Property="BorderBrush" Value="{StaticResource DefaultLightBorderBrush}"/> -->
        
        <!-- Для четкой границы -->
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
    </Style>

</ResourceDictionary>